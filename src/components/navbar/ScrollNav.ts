export const scrollHome = () => {
  window.scrollTo({
    top: 0,
    behavior: "smooth",
  });
};

export const scrollAbout = () => {
  window.scrollTo({
    top: 1600,
    behavior: "smooth",
  });
};

export const scrollProjects = () => {
  window.scrollTo({
    top: 3200,
    behavior: "smooth",
  });
};

export const scrollSkills = () => {
  window.scrollTo({
    top: 3000,
    behavior: "smooth",
  });
};

export const scrollStudies = () => {
  window.scrollTo({
    top: 3500,
    behavior: "smooth",
  });
};

